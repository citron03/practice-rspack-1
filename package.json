{
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "prune": "mkdir -p out && turbo prune --scope=host --docker"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.1",
    "turbo": "^2.5.4"
  },
  "packageManager": "pnpm@10.12.4"
}
